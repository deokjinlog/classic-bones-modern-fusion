# 기여 가이드 (Contributing)

`classic-bones-modern-fusion`은 **개인 포트폴리오 · 비상업** 프로젝트입니다. 그래도 이슈·PR·세팅팩 제안은 환영합니다. 아래만 지켜 주세요.

## 한 줄 요약

고전 플롯의 **뼈대**를 현대 **세팅**에 이식하되, "덜 쓰였다"를 **실데이터로 증명**하는 도구. 매칭은 코드(결정적), 융합만 LLM.

## 저장소 구조

| 경로 | 내용 |
|---|---|
| `docs/` | 스키마 정의 · 데이터 출처 · 세팅 초안 · 데이터 수집 리포트 |
| `data/corpus/` | 코퍼스 본체 (**`.gitignore` — 커밋 금지**) |
| `samples/` | 스키마 파일럿 (수기 라벨링 샘플) |
| `packs/` | 세팅팩 (예: `romcom.md`) |
| `exhibits/` | 배포 아티팩트 쇼케이스 (무서버 정적 HTML) |

## 시작하기

1. 코퍼스는 저장소에 없습니다. [`docs/data-sources.md`](docs/data-sources.md)의 URL로 직접 내려받아 `data/corpus/`에 두세요.
2. 원본 코퍼스는 **절대 커밋하지 마세요** (용량 + 라이선스). `.gitignore`로 막혀 있습니다.

## 기여할 수 있는 것

- **스키마 칸 제안/교정** — [`docs/schema.md`](docs/schema.md). 단, "표면 내용물"이 아니라 "옮겨지는 조건"만 (예: `왕좌` ❌ → `뺏을 권좌` ✅).
- **세팅팩 추가** — `packs/`에 새 현대 무대(병원·아이돌·물류…)를 25칸 스키마로 태깅해서.
- **뼈대 라벨 교정** — 샘플의 역할·턴·요구조건 오류 지적.
- **쇼케이스** — `exhibits/`에 결과를 보여주는 정적 HTML.

## 원칙 (PR 리뷰 기준)

1. **매칭은 취향 0** — 겹침 세기(집합연산)라 재현·검수 가능해야 한다. "느낌"으로 순위를 바꾸지 않는다.
2. **스키마는 조건이지 내용물이 아니다** — 세팅이 바뀌어도 옮겨지는 것만 칸으로 남긴다.
3. **근거 필수** — 신선(집거리)·검증(빈도)·양립(충족) 숫자 없이 프리미스만 내지 않는다.
4. **사용자 override가 이긴다** — 도구는 에디터지 게이트키퍼가 아니다. 약한 입력은 거절 대신 근거으로 투명하게, 필요하면 살을 붙여 보강 제안.

## 변경 제안

- 작고 초점 있는 PR로. 큰 변경은 이슈로 먼저 상의.
- 문서는 한국어, 파일명은 kebab-case.
- 커밋 메시지는 "무엇을 왜"가 한 줄에 보이게.

## 데이터 라이선스 주의

- **WikiPlots = CC-BY-SA**, **TVTropes = CC-BY-NC**. 기여물이 이 데이터를 포함하면 해당 라이선스를 따릅니다.
- 특히 **NC(비상업)** 조건 때문에 이 프로젝트는 비상업으로 유지합니다.
- 프로젝트가 직접 작성한 코드·문서 자체는 [MIT](LICENSE).
